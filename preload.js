//const path = require("path");
//const { contextBridge } = require('electron')


//const { requireTs } = require("require-ts-module");
//const tasksVisionModule = requireTs("@mediapipe/tasks-vision");



//const tsRequire = require('ts-require')();
//const tasksVisionModule = tsRequire(require.resolve("@mediapipe/tasks-vision"))
//console.log(path.resolve(__dirname, 'node_modules/@mediapipe/tasks-vision/vision_bundle.js'))
//const tasksVisionModule = tsRequire(path.resolve(__dirname, 'node_modules/@mediapipe/tasks-vision/vision_bundle'))

//const tasksVisionModule = require('@mediapipe/tasks-vision')


//import * as tasksVisionModule from '@mediapipe/tasks-vision'

/*
contextBridge.exposeInMainWorld('electron', {
    forVisionTasks: async (path) => await tasksVisionModule.default.FilesetResolver.forVisionTasks(path),
    createFromOptions: async (vision, options) => await tasksVisionModule.default.ImageSegmenter.createFromOptions(vision, options)
});
*/